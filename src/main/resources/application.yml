spring:
  datasource:
    url: jdbc:postgresql://34.151.230.206:5432/bidea
    username: leodev
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

external:
  api:
    discountValidator:
      url: https://sbv2bumkomidlxwffpgbh4k6jm0ydskh.lambda-url.us-east-1.on.aws/
      timeout: 5000
      retries: 3

resilience4j:
  circuitbreaker:
    instances:
      validateDiscount:
        registerHealthIndicator: true
        ringBufferSizeInClosedState: 5
        ringBufferSizeInHalfOpenState: 3
        waitDurationInOpenState: 10s
        failureRateThreshold: 50

  retry:
    instances:
      validateDiscount:
        maxAttempts: 3
        waitDuration: 5000ms

  timelimiter:
    instances:
      validateDiscount:
        timeoutDuration: 5s